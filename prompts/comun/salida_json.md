## FORMA DE RESPONDER

Responde **únicamente** con un objeto JSON válido que siga el esquema de abajo.
Sin texto antes, sin texto después, sin bloques de código, sin comentarios y sin
preguntas de seguimiento. Tu respuesta completa empieza con `{` y termina con `}`.
